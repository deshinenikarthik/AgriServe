spring.application.name=ADVISORY-SERVICE
server.port=8088

spring.datasource.url=jdbc:mysql://localhost:3306/advisoryservice_db

spring.datasource.username=root

spring.datasource.password=22311A0511

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

# Show all detailed logs for my specific project
logging.level.com.cognizant.agriserve=DEBUG

spring.config.import=optional:configserver:http://localhost:8888

# Register this service with Eureka
eureka.client.register-with-eureka=true

# Fetch the registry from Eureka to know about other services
eureka.client.fetch-registry=true
#
# Use IP address instead of hostname when registering
eureka.instance.prefer-ip-address=true
#
# The location of the Eureka Server
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

internal.service.key= MyClientSecretKey123!

spring.jackson.mapper.accept-case-insensitive-enums=true